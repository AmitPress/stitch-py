from setuptools import setup, find_packages

setup(
    name='stitch',
    version='0.1.0',
    packages=find_packages(),
    author="Amit Malaker",
    description="A tool to stitch together multiple python files into a single file (just like Rollup JS)",
)